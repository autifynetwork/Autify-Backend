server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root /var/www/html;

    index index.html index.htm index.nginx-debian.html;

    location /graphql {
        proxy_pass http://host.docker.internel:4000;
        proxy_redirect off;
        proxy_set_header Host $host;
    }
}